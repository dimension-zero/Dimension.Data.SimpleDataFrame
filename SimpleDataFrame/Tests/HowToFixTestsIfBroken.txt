Crib Sheet: Fix for Missing Assemblies in .NET Test

Separate NUnit and xUnit Tests: Keep NUnit and xUnit tests in separate classes. It’s fine to run the same tests in both, but ensure one class per framework.

Ensure Project File forces libraries to be copied to output directory:
  <PropertyGroup>
	  <CopyLocalLockFileAssemblies>true</CopyLocalLockFileAssemblies>
  </PropertyGroup>

Update Test SDK - must be correct version to match NUnit and xUnit:
dotnet add package Microsoft.NET.Test.Sdk --version <latest-version>

Add missing dependencies (e.g., Newtonsoft.Json, CsvHelper) via NuGet:
dotnet add package Newtonsoft.Json --version 13.0.1
dotnet add package CsvHelper --version <version>

Ensure All Dependencies Are Up-to-Date:
dotnet tool install --global dotnet-outdated
dotnet outdated --upgrade

Upgrade Specific Package:
dotnet add package Newtonsoft.Json --version *

Clean and Restore Project to Fresh Dependencies:
dotnet clean
dotnet restore

Clear MuGet cache to remove corrupted packages:
dotnet nuget locals all --clear
dotnet restore

Update Workloads = additional tools and libraries to extend .NET SDK for specific platforms, frameworks, use cases (mobile, web, cloud)
dotnet workload list
dotnet workload update

Run Tests in Isolation (skip build)
dotnet test --no-build --logger "console;verbosity=detailed"

Check Output Directory:
Verify the required .dll files are present in bin\Debug\netX.0\.


FULL SCRIPT
dotnet add package Microsoft.NET.Test.Sdk --version <latest-version>
dotnet add package Newtonsoft.Json --version 13.0.1
dotnet add package CsvHelper --version <version>
dotnet tool install --global dotnet-outdated
dotnet outdated --upgrade
dotnet add package Newtonsoft.Json --version *
dotnet clean
dotnet restore
dotnet nuget locals all --clear
dotnet restore
dotnet workload list
dotnet workload update
dotnet test --logger "console;verbosity=detailed"

